// Secrets.swift.example
// Copy this file to ReelRankings/Secrets.swift and paste in your own
// TMDB v3 API key (free at https://www.themoviedb.org/settings/api).
// ReelRankings/Secrets.swift is gitignored so the real key never lands
// in the repository.

enum Secrets {
    static let tmdbAPIKey = "YOUR_TMDB_API_KEY_HERE"
}
